'''
# 1: min or max problem

Q1 = input('enter min/max: ')
while Q1 == 'min':
    bla bla
while Q1 == 'max':
    bla bla

# 2: Enter obj func

obj = input('Enter the objective function in the format: f + 2x1 + 1x2 = 0 \n').split()

for i in obj:
    if type(i) == int:
        x.append(i)
    else:
        pass

# 3: Enter the constraints
C = [0,0]
C[0] = input('Constraint: ').split # have a one liner type checker to pick integers
C[1] = input('Constraint: ').split # have a one liner type checker to pick integers

C = []
while True:
    C3   = input('Constraint: ')
    if C3 == '':
        break
    else:
        assert ...
        C.append(C3)

# 4: build the matrix
        
# 5: Choose pivot column
if pivot column cannot be chosen print answer

        
# 6: Choose pivot row
# 7: Identify the basic and non-basic

# 8: Make basic column

# 9: back to step 5
''' 

    